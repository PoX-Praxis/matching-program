# --- 秘密情報（絶対にコミットしない / README 7章・仕様 7.2節） ---
.env
.env.*
*.key
secrets.json

# --- Python ---
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
env/

# --- 生成物（実行時に作られるもの） ---
# github_transition_builder.py の出力（本番でテーブルを実データ化したもの）
transition_table.json
# アプリの SQLite DB（本番は別管理）
pox.db
*.db
# ユーザーがアップロードした添付ファイル（ディレクトリだけ追跡）
static/uploads/*
!static/uploads/.gitkeep

# --- Qwen3 推論サーバ（実行時にモデルをDLするキャッシュ。コミットしない） ---
qwen3_server/.hf/
.hf/

# --- embedding 実験の生出力（評価表が成果物。生JSONは追跡しない） ---
eval/results/
# ベクトルキャッシュ（個人データ由来のベクトル。モデル別 .npz。git管理しない） ---
eval/cache/
# 分析出力（候補プロフィール本文を含むため git管理しない） ---
eval/analysis/

# --- Node.js ---
node_modules/
package-lock.json
package.json

# --- OS / エディタ ---
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
eval/github_profiles.jsonl

# solo_eval 用の個人プロフィール（個人情報なのでコミットしない）
eval/my_profile.json
